enum Size {
   Sm = "sm",
   Md = "md",
   Lg = "lg",
   Xl = "xl",
}

enum Color {
   Primary = "primary",
   Gray = "gray",
}

enum Variant {
   Solid = "solid",
   Outline = "outline",
   Link = "link",
}

enum Type {
   Submit = "submit",
   Reset = "reset",
   Button = "button",
}

export { Size, Color, Variant, Type };
